#include "monty.h"

/**
 * _stack - prints the stack's top
 * @head: the stack's head
 * @c: the line number
 * Return: no return
*/
void _stack(stack_t **head, unsigned int c)
{
	(void)head;
	(void)c;
	util.flag_change = 0;
}
